describe('registerEndpoint(endpoint, validations)', () => {
  test('api.json contains registered endpoints', () => {});

  test('api.json contains validation for endpoints', () => {});

  test('validations should be in json schema format');
});

describe('registerDiscovery(apiBasePath, api)', () => {
  test('exposes {apiBasePath}/.well-known/api.json', () => {});
});
